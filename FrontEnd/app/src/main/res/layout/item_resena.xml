<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardUseCompatPadding="true"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Nombre del usuario -->
        <TextView
            android:id="@+id/tvNombreResena"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textSize="15sp"
            android:textColor="@android:color/black"
            tools:text="Monse Gutiérrez" />

        <!-- Fecha de la reseña -->
        <TextView
            android:id="@+id/tvFechaResena"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@android:color/darker_gray"
            android:layout_marginTop="4dp"
            tools:text="2026-01-15" />

        <!-- Rating con estrellas -->
        <RatingBar
            android:id="@+id/ratingBarResena"
            style="?android:attr/ratingBarStyleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:isIndicator="true"
            android:numStars="5"
            android:rating="0"
            android:progressTint="#FFEB3B"
            android:progressBackgroundTint="#E0E0E0"
            android:secondaryProgressTint="#FFC107" />

        <!-- Comentario de la reseña -->
        <TextView
            android:id="@+id/tvComentarioResena"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textSize="14sp"
            android:textColor="#333333"
            android:lineSpacingExtra="2dp"
            tools:text="Excelente servicio, muy recomendado. El objeto estaba en perfectas condiciones." />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>