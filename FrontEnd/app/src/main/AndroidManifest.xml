<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Para Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Para Android 12 y anteriores -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/royazul"
        android:label="@string/app_name"
        android:roundIcon="@drawable/royazul"
        android:supportsRtl="true"
        android:theme="@style/Theme.ROY"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31"
        >





        <!-- Activity Principal (LAUNCHER) -->
        <activity
            android:name=".login.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <!-- Login y Registro -->
        <activity
            android:name=".login.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".login.Registro"
            android:exported="false" />

        <!-- Inicio (pantalla principal) -->
        <activity
            android:name=".Inicio"
            android:exported="false" />

        <!-- Objetos -->
        <activity
            android:name=".home.Objetoo"
            android:exported="false" />

        <activity
            android:name=".misobjetos.detalles"
            android:exported="false" />

        <activity
            android:name=".misobjetos.plantillaagregar"
            android:exported="false" />

        <!-- Solicitudes y Pagos -->
        <activity
            android:name=".solicitudes.metododepago"
            android:exported="false" />

        <activity
            android:name=".solicitudes.PayPalPaymentActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.example.roy" android:host="paypalpay" />
            </intent-filter>
        </activity>

        <activity
            android:name=".solicitudes.PagoExitosoActivity"
            android:exported="false" />

        <activity
            android:name=".solicitudes.PagoSimuladoActivity"
            android:exported="false" />

        <meta-data
            android:name="com.paypal.android.sdk.ClientId"
            android:value="Ad2Ti-cFyuNpJqkSu9EBQ21TlAjC3jhNtwXaujBU8NBX5t1cZeSIMe03RrTcnQMYzlZZmplVlRAkDxaL" />

    </application>

</manifest>